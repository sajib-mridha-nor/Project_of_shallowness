import 'package:flutter/material.dart';

class Pagese2 extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.teal,
        child: Text("Page2"),
        
      ),
    );
  }
}